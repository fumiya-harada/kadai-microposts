<ul class="list-unstyled">
    <% users.each do |user| %>
      <li class="media">
          <div>
            <%= user.content %>
          </div>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>