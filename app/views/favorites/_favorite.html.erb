<ul class="list-unstyled">
    <% users.each do |user| %>
      <li class="media">
          <div>
            <%= user.content %>
            <%= render 'favorites/favorite_button', micropost: user %>
          </div>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>